<script server>
    let error = null
    console.log('Processing request method:', request.method)
    if (request.method === 'POST') {
        const provider = body().provider
        console.log('Provider:', provider)
        if (provider) {
            try {
                // Disable autoRedirect so we can handle it explicitly
                const url = requestOAuth2Login(provider, {
                    redirectPath: '/auth/oauth/redirect',
                    autoRedirect: false
                })
                console.log('Redirecting to:', url)
                redirect(url)
            } catch (e) {
                console.error('Login error:', e)
                error = e.message
            }
        }
    }
</script>

<div class="min-h-screen bg-gradient-to-b from-base-200/50 via-base-100 to-base-100 flex flex-col items-center justify-center py-16 px-4">
    <div class="max-w-md w-full mx-auto space-y-8">
        <!-- Header -->
        <div class="text-center space-y-3">
            <div class="w-14 h-14 rounded-2xl bg-primary text-primary-content font-bold flex items-center justify-center mx-auto shadow-md">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z" />
                </svg>
            </div>
            <h1 class="text-3xl font-extrabold text-base-content tracking-tight">Create an Account</h1>
            <p class="text-sm text-base-content/60">
                Join Pocket Commerce to track orders and save your favorite items.
            </p>
        </div>

        <!-- Auth Card -->
        <div class="bg-base-100 border border-slate-200/80 dark:border-slate-800 rounded-3xl p-8 shadow-sm space-y-6">
            <!-- Primary Action: Google Sign In -->
            <form method="POST">
                <input type="hidden" name="provider" value="google">
                <button type="submit" class="btn btn-outline border-slate-300 dark:border-slate-700 w-full gap-3 font-semibold rounded-xl py-3 h-auto flex items-center justify-center hover:bg-base-200 text-base-content">
                    <svg class="h-5 w-5" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.06H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.94l2.85-2.22.81-.63z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.06l3.66 2.84c.87-2.6 3.3-4.52 6.16-4.52z"/>
                    </svg>
                    <span>Sign up with Google</span>
                </button>
            </form>

            <div class="divider text-xs font-medium text-base-content/40">OR</div>

            <div class="flex gap-3">
                <a href="/" class="btn btn-ghost btn-sm flex-1 rounded-xl font-semibold text-xs text-base-content/70">
                    ← Home
                </a>
                <a href="/login" class="btn btn-primary btn-sm flex-1 rounded-xl font-semibold text-xs">
                    Sign In Instead
                </a>
            </div>

            <p class="text-center text-[11px] text-base-content/40 pt-2 border-t border-slate-100 dark:border-slate-800">
                By signing up, you agree to our Terms of Service and Privacy Policy.
            </p>
        </div>
    </div>
</div>